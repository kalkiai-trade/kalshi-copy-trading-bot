# Contributing

1. Run `npm run typecheck` before opening a PR.
2. Describe trading-safety implications when changing order defaults or polling windows.
3. Prefer small commits with imperative messages (`feat:`, `fix:`, `chore:`, `docs:`).
